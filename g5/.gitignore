# Everything under out/ is generated by run-g5.sh: the fetched/patched agent jars, the
# recompiled release-11 demo classes, the manifests, and per-run stdout/stderr, JaCoCo
# exec+xml, bytecode dumps and assertion logs. It is ~300 MB, mostly OTel span logs.
# Reproduce with ./run-g5.sh.
out/
